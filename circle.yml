dependencies:
  override:
    - "rvm-exec 2.0.0-p598 bundle install --without metrics"
    - "rvm-exec 2.1.5 bundle install --without metrics"
    - "rvm-exec 2.2.0 bundle install --without metrics"

test:
  override:
    - "rvm-exec 2.0.0-p598 bundle exec rake"
    - "rvm-exec 2.1.5 bundle exec rake"
    - "rvm-exec 2.2.0 bundle exec rake"
